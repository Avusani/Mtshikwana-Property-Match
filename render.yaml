services:
  - type: web
    name: mtshikwana-property-match
    env: node
    buildCommand: npm install
    startCommand: npm start
    plan: free
    envVars:
      - key: NODE_VERSION
        value: 18.0.0
    buildFilter:
      paths:
        - public/**/*
        - server.js
        - package.json
